function show_magicians(magicians: string[]): void {
    for (let i = 0; i < magicians.length; i++) {
      console.log(magicians[i]);
    }
  }
  let magicians = ['Harry Housini', 'David Copperfiald', 'Penn Jillette', 'Tellar'];
  show_magicians(magicians);